name: Build/release
on:
  push:
    branches:
      - master
permissions:
  contents: write
jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v1

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v1
        with:
          node-version: 16

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build:win

      - name: Get version number
        id: get_version
        run: echo $(node -p "require('./package.json').version") >> $GITHUB_OUTPUT

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ join(steps.get_version.outputs.*, '') }}
          release_name: Release Version ${{ join(steps.get_version.outputs.*, '') }}
          files: |
            ./dist/*.exe
          draft: false
          prerelease: false